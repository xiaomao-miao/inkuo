# 质量：可观测性（本地优先）

## 1. 目标
- 便于定位崩溃、AI 调用失败、索引失败等问题。
- 默认仅本地保存，用户可选择导出。

## 2. 本地日志
- 分级：info/warn/error
- 脱敏：必须去除 API key、token、敏感文件内容

## 3. 导出
- 一键导出 zip（日志 + 配置摘要 + 版本信息）
- 导出前显示脱敏说明

## 4. 云端上报（可选）
- 仅会员功能可选开启
- 最小化：只上报必要指标，不传文档内容
